let x =
  "If you use double quote or single quote, " +
  "you can't break the line directly" +
  "You need ( + )";

let y = ` 
    You can't use break if using 
    double quote or single quote
`;
console.log(x);
//=>    You can't use break if using
//=>    double quote or single quote
